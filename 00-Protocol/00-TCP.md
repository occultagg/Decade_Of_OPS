# TCP

## 三次握手

1. 客户端发起SYN包给服务端
2. 服务端收到后返回一个SYN+ACK
3. 客户端收到ACK后再返回一个ACK,握手完成,建立链接

## 四次挥手

1. 客户端发出一个FIN,申请断开连接
2. 服务端返回ACK表示收到请求
3. 服务端没有数据要发了,就发一个FIN,表示它也可以断开链接了
4. 客户端返回一个ACK,等待2MSL时间,正式断开链接







